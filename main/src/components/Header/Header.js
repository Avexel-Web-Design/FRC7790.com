import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="header" id="home">
            <h1>Test Heading for Landing Page</h1>
        </header>
    );
};

export default Header;
